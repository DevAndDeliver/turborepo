# Flat node_modules layout (like Yarn's nodeLinker: node-modules).
# NestJS's DI relies on a single shared reflect-metadata instance; pnpm's
# default symlinked/strict node_modules can resolve multiple copies of it
# across packages, which breaks decorator metadata in non-obvious ways.
node-linker=hoisted
